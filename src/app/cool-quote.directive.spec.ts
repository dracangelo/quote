import { CoolQuoteDirective } from './cool-quote.directive';

describe('CoolQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
